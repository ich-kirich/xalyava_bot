export interface IPostInf {
  postId: number;
  postBlock: string;
  postContent: string;
  linksVideos: string[];
}

export interface IPost {
  postId: number;
  postText: string;
  imagesArray: string[];
}
